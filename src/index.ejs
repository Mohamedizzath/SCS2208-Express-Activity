<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link href="styles/bootstrap.css" rel="stylesheet" />
    <title>Home page</title>
</head>
<body>
    <div class="container pt-5">
        <div class="row">
            <div class="col">
                <img class="img-fluid" src="images/main-page-image.png" alt="main-page-image" />
            </div>
            <div class="col d-flex flex-column justify-content-center align-items-center">
                <h1>Welcome to our website</h1>
                <% if(email === undefined) { %>
                <h3>Your not logged to our system</h3>
                <a href="/login" class="btn btn-outline-dark mt-3">Login</a>
                <% } else { %>
                <h3>Your logged as <%= email %></h3>
                <% } %>
                <div class="container m-3">
                    <h3>Valid email address(password - 123)</h3>
                    <div class="container">
                        first@example.com<br/>
                        second@example.com<br/>
                        third@example.com<br/>
                    </div>
                </div>
                <p class="mt-2">Number of times visits to the site = <%= count %></p>
                <h3>Upload your files to the system(you need to login first)</h3>
                <a href="/uploads" class="btn btn-outline-warning">Upload</a>
            </div>
        </div>
    </div>
</body>
</html>